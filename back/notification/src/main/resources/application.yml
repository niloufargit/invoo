server:
  port: 7075
spring:
  application:
    name: notification
  data:
    mongodb:
      host: localhost
      port: 27018
      database: notification
      username: invoo
      password: password
      authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orchestratorGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  
  mail:
    host: localhost
    port: 1025
    username: invoo
    password: invoo
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
        